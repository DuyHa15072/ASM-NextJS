import React from 'react'
import { LayoutProps } from '../../model/layout'

type Props = {}

const Login = ({children}: LayoutProps) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default Login